"""
To Do:
create sparse feature vector for all the corpus
feature[dst][src] = sparseVector


"""
Features = {}
#features[src][dst] = sparseVector so each 2 words will have a dense vector according to the features
def CreateFeatures(file):
    """
    Features = ....
    """
