"""

run:
    CreateFeatures(file)
    runPerceptron


def runPerceptron(n):
    for each sentence:
        create goolden_tree
        create full graph
        myTree = Edmond(fullGraph)
    compare(goldenTree, myTree)

"""



